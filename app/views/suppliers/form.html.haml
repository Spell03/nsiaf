.page-header
  %h2= t("suppliers.title.#{params[:action]}")

.col-sm-9.column.col-sm-offset-1
  = simple_form_for @supplier, html: { class: 'form-horizontal' } do |f|
    = f.input :name
    = f.input :nit
    = f.input :telefono
    = f.input :contacto

    = render 'shared/form_buttons'
