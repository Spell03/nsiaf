.page-header
  %h2= t("ubicaciones.title.#{params[:action]}")

.col-sm-9.column.col-sm-offset-1
  = simple_form_for @ubicacion, html: { class: 'form-horizontal' } do |f|
    = f.input :abreviacion
    = f.input :descripcion

    = render 'shared/form_buttons'
