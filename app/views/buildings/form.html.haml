.page-header
  %h2= t("buildings.title.#{params[:action]}")

.col-sm-9.column.col-sm-offset-1
  = simple_form_for @building, html: { class: 'form-horizontal' } do |f|
    = f.input :code
    = f.input :name
    = f.association :entity, include_blank: false

    = render 'shared/form_buttons'
